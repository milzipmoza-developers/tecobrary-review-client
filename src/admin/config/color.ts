import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    secondary: {
      light: 'rgb(85, 85, 85)',
      main: 'rgb(60, 63, 65)',
      dark: 'rgb(49, 51, 53)',
      contrastText: '#FFF',
    },
  },
});